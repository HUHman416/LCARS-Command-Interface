LCARS Universal Linux Desktop Version 31.5 Development
Application version: 31.5.0
AppImage SHA-256: GENERATED-DURING-PACKAGING

The installer verifies this checksum before replacing the installed application.
The optional LCARS login session is installed only from Settings with explicit authorization. It retains a normal-desktop escape route.
Version 31.5 Development adds Daily Utilities for private clipboard history, operator-approved capture, application notifications, and print-queue management. It also repairs trusted Voice Control microphone authorization and duplicate unmute requests while retaining the complete Version 30 Stable environment.
Run install-autostart.sh to register a portable AppImage in application search and optionally enable login startup without sudo.
